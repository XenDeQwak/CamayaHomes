package com.xen.camaya.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.xen.camaya.entity.PropertyImageData;

public interface PropertyImageRepository extends JpaRepository<PropertyImageData, Integer> {

}
